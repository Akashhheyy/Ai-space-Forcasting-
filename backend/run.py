"""Entry point for the Space Radiation Forecast API."""

import uvicorn

from app.utilities.config import settings

if __name__ == "__main__":
    uvicorn.run(
        "app.main:app",
        host=settings.host,
        port=settings.port,
        reload=settings.debug,
    )
